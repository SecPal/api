# SPDX-FileCopyrightText: 2025 SecPal
# SPDX-License-Identifier: CC0-1.0

includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: max
    paths:
        - app
        - config
        - database
        - routes
        - tests

    # PHPStan performance configuration
    tmpDir: build/phpstan
    parallel:
        maximumNumberOfProcesses: 4
        processTimeout: 300.0

    # Ignore errors in generated files
    excludePaths:
        - bootstrap/cache/*
        - storage/*
        - vendor/*

    # Laravel specific
    noUnnecessaryCollectionCall: true
    noUnnecessaryCollectionCallOnly: []
    noUnnecessaryCollectionCallExcept: []

    # Ignore errors in vendor migrations
    ignoreErrors:
        -
            message: '#Cannot access offset .* on mixed#'
            path: database/migrations/*_create_permission_tables.php
        -
            message: '#Parameter .* expects .*, mixed given#'
            path: database/migrations/*_create_permission_tables.php
        -
            message: '#Call to an undefined method PHPUnit\\Framework\\TestCase::getJson\(\)#'
            path: tests/Feature/*
        -
            message: '#Cannot call method assert.*\(\) on mixed#'
            path: tests/Feature/*
