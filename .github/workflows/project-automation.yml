# SPDX-FileCopyrightText: 2025-2026 SecPal Contributors
# SPDX-License-Identifier: CC0-1.0

---
name: Project Board Automation

on:
  issues:
    types:
      - opened
      - reopened
      - closed
      - labeled
  pull_request:
    types:
      - opened
      - ready_for_review
      - closed
      - converted_to_draft
      - labeled
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    name: Project automation
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@76d07ca0d9bd796896de1c05c3307d66e90cdc6a
    secrets: inherit

  draft-reminder:
    name: Draft PR Reminder
    if: github.event_name == 'pull_request'
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@76d07ca0d9bd796896de1c05c3307d66e90cdc6a
    permissions:
      issues: write
      pull-requests: write
