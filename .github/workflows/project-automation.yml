# SPDX-FileCopyrightText: 2025-2026 SecPal Contributors
# SPDX-License-Identifier: CC0-1.0

---
name: Project Board Automation

on:
  issues:
    types:
      - opened
      - reopened
      - closed
      - labeled
  pull_request:
    types:
      - opened
      - ready_for_review
      - closed
      - converted_to_draft
      - labeled
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    name: Project automation
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@541ab1c531a71efdf3607b42e1cf69fde539db30
    secrets: inherit

  draft-reminder:
    name: Draft PR Reminder
    if: github.event_name == 'pull_request'
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@541ab1c531a71efdf3607b42e1cf69fde539db30
    permissions:
      issues: write
      pull-requests: write
