# SPDX-FileCopyrightText: 2025-2026 SecPal Contributors
# SPDX-License-Identifier: CC0-1.0

---
name: Project Board Automation

on:
  issues:
    types:
      - opened
      - reopened
      - closed
      - labeled
  pull_request:
    types:
      - opened
      - ready_for_review
      - closed
      - converted_to_draft
      - labeled
  pull_request_review:
    types:
      - submitted

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    name: Project automation
    uses: SecPal/.github/.github/workflows/project-automation-core.yml@e3cdfac353f67af8350dd4e84064fdc7993e2988
    secrets: inherit

  draft-reminder:
    name: Draft PR Reminder
    if: github.event_name == 'pull_request'
    uses: SecPal/.github/.github/workflows/draft-pr-reminder.yml@e3cdfac353f67af8350dd4e84064fdc7993e2988
    permissions:
      issues: write
      pull-requests: write
